Generating Bluesky plan for: 'move motor to 360 degrees'
Using provider: cborg
================================================================================
BLUESKY PLAN: move_motor_to_degrees
================================================================================
Description: Moves a motor to a specified position of 360 degrees using an absolute positioning command

Estimated Duration: 5-30 seconds

Required Devices:
  - motor

Parameters:
  - motor: EpicsMotor device to be moved
  - position: Target position in degrees (default: 360)

Plan Code:
----------------------------------------
import bluesky.plans as bp
import bluesky.plan_stubs as bps
import bluesky.preprocessors as bpp
from ophyd import EpicsMotor

# Device definition
motor = EpicsMotor('PV:motor', name='motor')

@bpp.stage_decorator([motor])
@bpp.run_decorator()
def move_motor_to_degrees(motor, position=360):
    """
    Move motor to specified position in degrees.
    
    Parameters
    ----------
    motor : EpicsMotor
        Motor device to move
    position : float, optional
        Target position in degrees (default: 360)
        
    Yields
    ------
    Msg
        Bluesky messages for motor movement
    """
    # Move motor to specified position
    yield from bps.mv(motor, position)
    
    # Optional: read the motor position to confirm
    yield from bps.read(motor)
----------------------------------------
